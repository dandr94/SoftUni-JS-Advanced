function setProperties(name, population, treasury) {
  let data = {
    name,
    population,
    treasury,
  };

  console.log(data);
}

setProperties("Tortuga", 7000, 15000);

setProperties("Santo Domingo", 12000, 23500);
